In this project we had to use the "win-x86" version of e_sqlite3.dll.
We tried to use the "runtimes" folders 
(e.g. "runtimes\win-arm64\native\e_sqlite.dll") 
but it didn't work and we couldn't understand why.